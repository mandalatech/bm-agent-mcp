{
  "name": "bm-agent-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Books Mandala Agent MCP Server — AI agents can search, browse, and discover books",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "agents": "^0.5.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260228.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.47.0"
  }
}
